import Typography from "typography";
// import grandViewTheme from "typography-theme-grand-view";

const typography = new Typography({ baseFontSize: "18px", bodyFontFamily: ['Muli'] });

export default typography;